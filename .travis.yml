language: cpp

compiler:
    - clang
    - gcc

os:
    - linux
    - osx

install:
    - mkdir build
    - cd build
    - cmake ..
    - make

script: make test

notifications:
    on_success: change
    on_failure: always

branches:
    only:
        - master
